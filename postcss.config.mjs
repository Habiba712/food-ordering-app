const config = {
  plugins: ["@tailwindcss/postcss"],
  theme: {
    extend: {
      colors: {
        primary: "red", // Custom primary color (change as needed)
        secondary: "#9333EA",
        accent: "#F59E0B",
        dark: "#1E293B",
        light: "#F3F4F6",
      },
    },
  },
};

export default config;
